#ifndef HUFFMAN
#define HUFFMAN

#include <iostream>
#include <fstream>
#include <string>
#include <algorithm>
#include <climits>
#include <cstdint>

typedef unsigned char BYTE;
const size_t ALPHABET_SIZE = (1 << sizeof(BYTE)*CHAR_BIT); 

#endif
